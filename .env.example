# Environment variables for the OGSTEP QC Dashboard
#
# How to use:
# 1. Copy this file to a new file named `.env` in the same directory.
# 2. In the `.env` file, fill in the values for the variables below.
# 3. The `.env` file is ignored by Git, so your secrets are safe.

# The ID of the Google Sheet to use as the data source.
# This is the long string of characters in the middle of the sheet's URL.
# Example: "1aBcDeFgHiJkLmNoPqRsTuVwXyZ"
VITE_GOOGLE_SHEET_ID=""

# The name of the sheet (tab) to use within the Google Sheet.
# If not provided, it will default to "Form Responses 1".
# Example: "Survey Data"
VITE_GOOGLE_SHEET_NAME=""

# Optional: The numeric sheet GID from the Google Sheet URL.
# If provided, it takes precedence over VITE_GOOGLE_SHEET_NAME.
# Example: "123456789"
VITE_GOOGLE_SHEET_GID=""
